import React from 'react';
import { Layout, HeaderText } from '../styles';

const Header = () => (
  <Layout paddingBottom="5rem">
    <HeaderText>Youtube Channel Dropdown List </HeaderText>
  </Layout>
);

export default Header;
